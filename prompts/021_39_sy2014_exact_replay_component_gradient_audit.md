# 021_39 SY2014 021_35精确重放与分项梯度审计

## 目的

021_35未保存agent/TDn/margin分项梯度。本任务精确重放同一seed0、同一1K流程，仅增加autograd诊断日志，补齐三项梯度范数和两两余弦。

## 强制边界

- 训练逻辑、采样、随机种子、reward、IC、动作、预算、target冻结全部与021_35相同；
- 只增加`torch.autograd.grad(..., retain_graph=True)`日志，不改变总loss和optimizer.step；
- 必须逐checkpoint复现021_35的yield/I/N/late-N；最大绝对误差必须为0，否则诊断作废；
- 不扩展超过1K，不调任何权重。

## 预注册判据

collapse=step50–500，recovery=501–1000：

- A：任一分项梯度范数中位数比>=2或<=0.5，或任一两两梯度余弦的阶段差绝对值>=0.2；存在时段性梯度候选；
- B：所有范数比在0.5–2且所有余弦差<0.1；分项梯度无时间区分力；
- C：其他混合结果。

本任务仍不允许把相关变化写成因果根因。

