# 026_11 五站点物理深度统一观测 adapter 离线单测记录

## 结果

`passed`，37/37 项检查通过。任务边界为纯离线映射与守恒单测：0训练、0 DSSAT调用，不接入现有 SY 权重。

公共深度带为 0–15、15–30、30–60、60–90、90–120、120–150 cm。每带同时输出厚度加权 `sw_vwc` 和 `coverage_fraction`，最终固定为28维观测。

五站点均满足：

- 已覆盖范围的 `sum(sw × layer_thickness)` 映射前后守恒；
- 非土壤16个特征逐位保留；
- 覆盖率始终位于0–1；
- HLA/YC的90 cm以下保持 `(sw=0, coverage=0)`，没有外推；
- SY/HLA/YC/FQ/LC 的源计算深度分别保持100/90/90/100/150 cm。

## 边界

该结果只证明输入表示可统一，不证明同一策略能跨站点决策。现有 SY 模型是25维，不能加载到28维环境；后续必须重新训练。

进入联合训练前还需审计第二个结构问题：当前 SY 阶段点 DAP 1/30/50/65/85/110 是否在五站点代表相同物候意义。若不一致，应改为物候触发，而不是把相同绝对DAP强加给所有站点。
