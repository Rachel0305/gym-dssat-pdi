# 028_12 已筛选年份代表策略五情景汇报包

## 目标

为 028_03 的 17 个已筛选站点—年份建立可直接向导师汇报的统一证据包：全部 seed 结果总表、每年一个预注册代表 seed、五情景终值、逐日表、降雨/气温/土壤水分/水氮胁迫/管理事件/两种产量/累积奖励图，以及模型—数据—图 manifest。

## 代表 seed 规则

不得事后只挑好看的图。按以下固定次序选择：

1. `advisor_any_metric_win=True` 优先；
2. 严格领先指标数量更多优先；
3. 仍并列时，未领先指标中最差百分比缺口更小优先；
4. 仍并列时 seed 编号更小优先。

所有未被选中的 seed 仍保留在总表，跨 seed 稳定性不得隐藏。

## 不重复原则

- 复用 027_05、028_04、028_05 已有日值、图和 manifest。
- HLA/YC2008/FQ 跨年迁移直接使用 028_08/09/11 已保存 snapshot，不重跑。
- FQ2016 代表 seed2 的训练期评估 snapshot 当时未持久化，允许且仅允许一次固定模型确定性复评以补日值；不得训练或重选 checkpoint。

## 输出

- 17 年代表策略 registry。
- 全 seed 总览与跨 seed 通过率。
- 17×5 情景终值总表与合并逐日表。
- 每个新增 case 的 PNG/SVG endpoint、日过程和阶段动作图。
- advisor-ready Markdown 记录。

