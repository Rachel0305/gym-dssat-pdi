# 2026-08-12 YC ent_coef=0.02 rescue

## Execution

- Container: `nifty_taussig`; interpreter `/opt/gym_dssat_pdi/bin/python`.
- Only intervention: `ent_coef=0.02`; all other PPO, input, years, seed, reward, safety, observation and action-grid settings matched the registered YC baseline.
- 25,000 steps; checkpoints 5K, 10K and 25K; isolated output `benchmark_results/066_01_yca_lowIC_ent002_rescue25k_maskableppo_smoke2k`.
- Runtime about 273 s; no OOM; no 100K.

## Validation means

| checkpoint | yield kg/ha | irrigation mm | N kg/ha | PFP_N | mechanism |
|---:|---:|---:|---:|---:|---|
| 5K | 7392.35 | 231 | 240 | 30.80 | not selected |
| 10K | 6758.04 | 132 | 240 | 28.16 | not selected |
| 25K | 8197.39 | 228 | 240 | 34.16 | fail: one cross-year action signature |

All ten daily outputs, action-grid checks and request/safe/DSSAT transmission checks passed. The 25K endpoint did not collapse in yield and retained post-DAP1 actions, but all ten validation years shared one action signature. Under the preregistered stability gate this is not a generalizable policy, so the branch is stopped and 100K is forbidden. `WP_ET` was not available from the 2K/25K training summary and was not inferred.

## Full-metric replay status

The renderer/input-root repair is documented in `docs/2026-08-12_phase0_full_metric_replay.md`. This ent_coef=0.02 branch was not included in that six-candidate phase-0 replay, so no corrected five-scenario `WP_ET` value is claimed here. The training-summary values above remain the only reported metrics for this branch; `WP_ET` is unavailable and was not inferred. The mechanism failure (one cross-year action signature) remains sufficient to stop the branch and forbid 100K.
